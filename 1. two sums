class Solution:
    def twoSum(self, nums, target):
        """
        :type nums: List[int]
        :type target: int
        :rtype: List[int]
        """
        complement={}
        for i,i_val in enumerate(nums):
            if i_val in complement:
                return [complement[i_val],i]
            else:
                complement[target-i_val]=i
        return [-1,-1]
